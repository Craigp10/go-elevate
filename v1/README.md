## Version 1

This version of go-elevate will aim to solve the elevator problem. Move an object from 1 floor to the other. Input to the program will be the floor the elevator is on and the floor to move to. The program will calculate distance and move at a rate of 1 floor/second.

example run
`go run main.go --version=v1 10 4`


